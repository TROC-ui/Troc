# URL absolue du backend déployé (Render). Laisser vide en local : le proxy
# Vite (vite.config.js) redirige déjà /api vers http://localhost:3000.
VITE_API_URL="https://troc-backend.onrender.com/api"

# Même backend, pour la connexion Socket.io (messagerie temps réel).
VITE_SOCKET_URL="https://troc-backend.onrender.com"
